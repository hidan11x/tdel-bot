[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "fontconfig", "freetype", "libpng", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python start_prod.py"
